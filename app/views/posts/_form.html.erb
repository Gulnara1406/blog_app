<%= form_with model: @post do |form| %>
  <p><%= form.label :body, 'Текст записи' %></p>
  <p><%= form.text_area :body, rows: 5, class: 'form-text-area'%></p>
  <p class="mt"><%=form.submit 'Опубликовать', class: 'button button-main'%></p>
<%end%>